
import React from "react";

// This layout is part of a deprecated file structure and now simply passes children through.
export default function DeprecatedInventoryLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
